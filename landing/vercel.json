{
  "framework": "angular",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd .. && pnpm --dir packages/shared run build && pnpm --dir landing run build",
  "outputDirectory": "dist/pockly/browser"
}
