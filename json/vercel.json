{
  "framework": "angular",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd .. && pnpm --dir packages/shared run build && pnpm --dir json run build",
  "outputDirectory": "dist/json/browser"
}
