{
  "framework": "angular",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../packages/shared && pnpm run build && cd ../text && pnpm run build",
  "outputDirectory": "dist/text/browser"
}
