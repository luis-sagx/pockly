{
  "framework": "angular",
  "installCommand": "corepack enable && corepack prepare pnpm@10.28.0 --activate && cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.28.0 --activate && cd .. && pnpm --dir packages/shared run build && pnpm --dir text run build",
  "outputDirectory": "dist/text/browser"
}
