{
  "framework": "angular",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd .. && pnpm --dir packages/shared run build && pnpm --dir calculator run build",
  "outputDirectory": "dist/calculator/browser"
}
