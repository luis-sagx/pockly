{
  "framework": "angular",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../packages/shared && pnpm run build && cd ../calculator && pnpm run build",
  "outputDirectory": "dist/calculator/browser"
}
