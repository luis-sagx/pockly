// TODO: Extract canonical SeoService from text app (most mature version).
// Per-app PAGE_CONFIGS to be injected via provider token.
// Pending consolidation as part of P2-3 workspace dedup.
export class SeoService {}
