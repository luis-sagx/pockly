{
  "framework": "angular",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../packages/shared && pnpm run build && cd ../json && pnpm run build",
  "outputDirectory": "dist/json/browser"
}
