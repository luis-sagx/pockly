{
  "framework": "angular",
  "buildCommand": "cd ../packages/shared && pnpm run build && cd - && pnpm run build",
  "outputDirectory": "dist/pockly/browser",
  "installCommand": "pnpm install --frozen-lockfile"
}
