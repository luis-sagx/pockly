{
  "framework": "angular",
  "installCommand": "cd .. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../packages/shared && pnpm run build && cd ../landing && pnpm run build",
  "outputDirectory": "dist/pockly/browser"
}
